USB=sdb1

mkdir -p /mnt/{usb,iso}
mount -o loop archlinux-2013.03.01-dual.iso /mnt/iso

awk 'BEGIN {FS="="} /archisolabel/ {print $3}' /mnt/iso/loader/entries/archiso-x86_64.conf | xargs mkfs.vfat -F32 /dev/$USB -n

mount /dev/$USB /mnt/usb
cp -a /mnt/iso/* /mnt/usb
sync
umount /mnt/{usb,iso}



